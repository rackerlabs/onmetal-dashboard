extends layout

block content
  .container
    h1 Ironic Dashboard
    .content
      table.table.table-striped
        thead
          tr
            th Region
            th Ironic URL
        tbody
          each region in regions
            tr
              td <a href="/#{region.name}">#{region.name}</a>
              td #{region.url}
